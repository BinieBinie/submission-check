# submission-check 스킬 수정 이유

## 배경
전역 스킬(`~/.hermes/skills/productivity/submission-check/SKILL.md`)을 프로젝트 폴더(`submission-check-hermes/`)에 `skill.md`로 가져왔다.
프로젝트는 이 스킬을 Next.js 앱(`lib/llm.ts`, `lib/prompt.ts`, `lib/mock.ts`, `/api/inspect`)으로 구현한 형태다.
따라서 전역 스킬의 규칙을 프로젝트 구현과 맞춰 점검할 필요가 있다.

## 수정 대상 (고치기 전 상태: `skill.original.md`)
SKILL.md 제한 규칙에 다음 문장이 있다.

```
외부 API나 사용자 인증정보는 사용하지 않는다. 제출 요건 대조 및 판정을 위한 Solar Pro 4 호출은 이 스킬 실행의 일부로 사용하며, 그 밖의 외부 API·사용자 계정·토큰 등은 사용하지 않는다.
```

## 이 문장을 고쳐야 하는 이유

### 1. 실제 Solar Pro 4 호출이 이미 구현되어 있다
- `lib/llm.ts`는 `https://api.upstage.ai/v1/chat/completions`로 `solar-pro4` 모델을 호출하는 `callSolarPro4` 함수를 정의한다.
- `/api/inspect` 라우트와 `app/page.tsx`는 `mockMode`가 꺼져 있고 `UPSTAGE_API_KEY`가 있을 때 이 실제 호출을 사용한다.
- 즉 이 프로젝트는 **외부 API(Solar Pro 4)를 쓰는 것이 전제**다.

### 2. "외부 API를 사용하지 않는다"와 충돌한다
- 위 SKILL.md 문장은 "외부 API를 사용하지 않는다"고 먼저宣言하면서, 바로 뒤에 "Solar Pro 4 호출은 이 스킬 실행의 일부로 사용한다"고 적고 있다.
- 문장 자체로는 Solar 호출만 예외로 두는 구조지만, "외부 API를 사용하지 않는다"는 앞선 선언이 프로젝트 구현(실제 Solar 호출)과 상충해 보인다.
- 특히 이 프로젝트가 **mockMode**로 "키 없이 흐름만 확인"하는 실행 모드를 갖고 있는 점과, 이 문장의 표현이 잘 맞지 않는다. mockMode일 때는 실제 Solar 호출이 일어나지 않으므로, "Solar Pro 4 호출은 이 스킬 실행의 일부로 사용"이라는 서술은 **조건 없이 항상 호출하는 듯한 인상**을 준다.

### 3. 프로젝트 맥락에서 다듬을 방향 (후속 수정용 메모)
- "외부 API나 사용자 인증정보는 사용하지 않는다" 부분은 이 프로젝트에서는 사실이 아니므로, 프로젝트용 `skill.md`에서는 삭제하거나 다른 표현으로 바꿔야 한다.
- Solar Pro 4 호출에 대해서는 **mockMode가 꺼져 있고 API 키가 있을 때만 실제 호출**한다는 조건을 명시하는 쪽이 실제 동작과 맞는다.
- "그 밖의 외부 API·사용자 계정·토큰 등은 사용하지 않는다" 부분은 제출 요건 대조 목적 외의 외부 호출을 막는 취지이므로, 이 프로젝트의 범위에서는 유지할 수 있다. 다만 앞의 "외부 API 사용 안 함"과 끊어서 읽히게 정리해야 한다.

## 함께 볼 문제: 판정값 불일치
SKILL.md 3단계는 판정값을 **[충족] / [불충분] / [미충족]**으로 규정한다.
그런데 프로젝트 코드의 판정값은 이와 다르다.

- `lib/prompt.ts` 출력 형식: `"result": "확인됨"`
- `lib/prompt.ts` 판정 규칙 주석: `"확인됨" / "일부만 확인됨" / "확인되지 않음"`
- `lib/mock.ts` 반환값: `result: '확인됨'` / `'일부만 확인됨'` / `'확인되지 않음'`

즉 SKILL.md가 요구하는 세 값과 프로젝트 구현이 쓰는 세 값이 **다른 집합**이다.
이 불일치는 SKILL.md를 프로젝트컨텍스트에서 그대로 쓸 수 없는 또 하나의 이유이며, 어떤 쪽을 기준으로 통일할지 결정해야 한다.

## 다음 단계
1. `skill.original.md`는 고치기 전 원문 보존용으로 남겨둔다.
2. `skill.md`를 프로젝트 실제 동작(mockMode + 실제 Solar 호출 + 판정값)에 맞춰 수정한다.
3. 수정 범위는 적어도 다음 두 군이다.
   - 제한 규칙의 API 관련 문장
   - 판정값 정의([충족]/[불충분]/[미충족] vs 확인됨/일부만 확인됨/확인되지 않음)
