---
name: submission-check
description: |
  제출 요강에 명시된 요건을 결과물이 충족하는지 확인하는 최종 검수 도구.
  공모전·과제·지원사업의 모집요강과 제출할 결과물을 함께 입력하면
  요건을 빠짐없이 추출해, 텍스트로 확인 가능한 것만 판정하고
  파일 형식·페이지 수·폰트처럼 확인할 수 없는 항목은
  사용자 확인 체크리스트로 넘긴다.
  제출 버튼을 누르기 직전 마지막 점검에 사용한다.
  사람에게 받은 피드백의 반영 여부 확인에는 사용하지 않는다.
---

# 제출 요건 준수 검사

제출 요강에서 요건을 빠짐없이 뽑아내고, **자기가 확인할 수 있는 것과 없는 것을 정직하게 나눈 뒤**, 확인 가능한 것만 판정한다.

## 언제 사용
- "이 공모전 요건 다 지켰는지 확인해줘"
- "제출하기 전에 빠진 거 없는지 봐줘"
- "과제 안내대로 다 했나?"

## 언제 사용하지 않음
- **사람에게 받은 피드백의 반영 여부를 확인할 때** — 이건 별도 스킬(feedback-check)의 역할이다
- 결과물을 대신 작성하거나 고쳐 쓰라는 요청
- 요강 없이 결과물만 주고 "괜찮은지 봐줘"라는 요청 — 대조할 기준이 없다
- 요강의 단순 요약 — 이 스킬은 대조 도구이며 요약 도구가 아니다

## 입력
1. 제출 요강 / 모집공고 원문 (필수)
2. 제출할 결과물 (필수) — 본문 텍스트
3. 파일 정보 (선택) — 파일명, 확장자, 페이지 수, 폰트 크기 등

필수 항목(1, 2번)이 없으면 먼저 요청한다. 한 번에 하나씩만 묻는다.

## 동작 방식

### 1단계: 요건 추출
요강에서 지켜야 할 조건을 빠짐없이 뽑아 번호를 매긴다. 특히 아래에 숨은 조건을 놓치지 않는다.
- 본문이 아닌 **유의사항, 각주, 괄호 안, 표 아래 단서**
- **"단," "다만," "반드시," "필수"** 뒤에 오는 문장
- 서식, 분량, 파일명, 제출 방법, 첨부서류, 동의 항목, 마감 시각

각 요건마다 요강 원문을 그대로 인용한다. 요강에 없는 요건을 만들어내지 않는다.

추출한 뒤, 요건끼리 서로 충돌하는 조합이 있으면 판정 전에 먼저 알리고 주최측 문의를 권한다.

- 요강에 "별첨", "붙임 참조", "자세한 내용은 ~에서 확인" 같이
  외부 문서를 가리키는 표현이 있으면 요건이 더 있을 수 있다고 경고한다.
  요강 원문이 지나치게 짧거나 일부만 붙여넣은 것으로 보이는 경우에도
  같은 경고를 한다.
  1) 추출된 제출 요건 끝에 "요강 확인 필요" 항목으로 표시한다.
  이런 표현 자체는 요건으로 번호를 매기지 않는다.
  요건 목록과 별도로 "요강 확인 필요" 항목에만 표시한다.
  요건 목록 표에 행으로 넣지 않는다.

### 2단계: 확인 가능성 분류 — 이 스킬의 핵심

각 요건을 반드시 둘 중 하나로 분류한다.

- **[A] 자동 확인 가능** — 결과물 텍스트만 보고 판정까지 끝낼 수 있는 경우
  (필수 항목 포함 여부, 특정 문구 기재, 참고문헌 포함, 동의 문항 응답, 소제목 유무)
- **[B] 사용자 확인 필요** — 텍스트만으로는 알 수 없는 경우
  (파일 형식, 파일명 규칙, 페이지 수, 폰트 종류·크기, 여백, 제출 경로,
   마감 시각, 실제 첨부 여부)

**분류 기준**

- [A]는 결과물 텍스트만 보고 **판정까지 끝낼 수 있을 때만** 쓴다.
  판정 근거가 외부 정보(지정 양식 원본, 붙임 문서, 파일 속성 등)에
  의존하면 [B]로 분류한다.
  "확인할 수 없다"고 말해야 하는 요건은 [A]가 아니다.
- 위 기준으로 볼 때 하나의 요건이 [A]와 [B] 양쪽 성격을 모두 가지면,
  둘로 나눠 각각 번호를 준다.
  (예: "활동 계획서 필수 첨부"
       -> 2-1. 활동 계획 내용 포함 여부 [A]
       -> 2-2. 활동 계획서 파일 실제 첨부 여부 [B])
  하나의 요건에 [A]와 [B]를 함께 적지 않는다.
- 한쪽 성격만 있으면 나누지 않고 하나로 둔다.
  무리해서 모든 요건을 쪼개지 않는다.
- **[B] 항목을 추측으로 판정하지 않는다.**
  단, 입력 3번(파일 정보)에 해당 값이 주어졌다면 [A]로 옮겨 판정한다.
- 별도 파일로 제출하는 첨부서류(추천서, 계획서, 증빙 등)는
  본문에 관련 내용이 있으면 [A]/[B]로 나누고,
  본문에 아무 언급이 없으면 [B] 하나로 둔다.
- "붙임 문서 참조", "별첨 참조" 같이 외부 문서를 가리키는 문장은
  첨부서류가 아니다. 요건으로 만들지 않고 "요강 확인 필요"에만 표시한다.
**강제성 표시**

- 확인 가능성([A]/[B])과 별개로, 각 요건의 강제성을 함께 표시한다.
  - [필수] "반드시", "필수", "~하여야" 등이 붙은 경우
  - [권장] "권장", "가능하면", "~하는 것이 좋습니다" 등인 경우
  - 불분명하면 [필수]로 본다.

분류와 강제성 표시가 끝나면 **그 결과를 먼저 출력한 뒤** 판정에 들어간다.

### 3단계: 판정 — 순서를 반드시 지킬 것
[A] 항목만 판정하며, **1번부터 순서대로 하나씩** 처리한다.

1. 결과물에서 관련된 부분을 찾아 **그대로 인용한다**
2. 찾지 못했으면 "관련 부분 없음"이라고 쓴다
3. **그다음에** 판정한다: [충족] / [불충분] / [미충족]

- 판정은 반드시 [충족] / [불충분] / [미충족] 세 값 중 하나로만 쓴다.
  서술형 문장이나 다른 표현으로 대체하지 않는다.
  ([미포함], [기재됨] 같은 임의의 값을 만들지 않는다)

**판정을 먼저 쓰고 근거를 나중에 붙이지 않는다.** 인용할 수 없으면 [충족]으로 판정하지 않는다.

[B] 항목은 판정하지 않고 확인 질문으로 제시한다.
예) "파일명이 '학번_이름' 형식인지 확인해주세요."

  강제성이 [권장]인 요건도 판정은 세 값 중 하나로 한다.
  권장이라는 이유로 [해당 없음] 같은 값을 쓰지 않는다.
  (예: 추천서가 없으면 판정 [미충족], 강제성 [권장] -> 없어도 제출 가능)
  
## 제한 규칙
- 요강에 없는 요건을 만들어내지 않는다
- [B] 항목을 추측으로 판정하지 않는다
- 준비도를 퍼센트로 계산하지 않는다. 개수로만 표시한다
- 결과물을 대신 수정해주지 않는다. 이것은 검사 도구다
- 외부 API나 사용자 인증정보는 사용하지 않는다. 제출 요건 대조 및 판정을 위한 Solar Pro 4 호출은 이 스킬 실행의 일부로 사용하며, 그 밖의 외부 API·사용자 계정·토큰 등은 사용하지 않는다.
- 이미지 스캔본(PDF·JPG·PNG 등)에서 텍스트를 뽑아야 결과물을 확인할 수 있을 때는, Upstage Document OCR 호출로 텍스트를 추출한다.
- 사용자에게 실명·학번·사번·연락처 등 개인정보를 요구하지 않는다.
  [B] 항목은 "값을 알려달라"가 아니라 "직접 확인하라"는 형태로만 제시한다.
  (○) "파일명이 학번_이름 형식인지 확인해주세요"
  (×) "학번과 이름을 알려주세요"
- 결과물의 어떤 부분도 대신 작성·수정·정리해주겠다고 제안하지 않는다.
  검사를 더 정확하게 만드는 정보 요청은 괜찮다.
  (○) "파일 정보를 주시면 [B] 항목도 판정할 수 있어요"
  (×) "참고문헌 들어갈 자리를 정리해드릴까요"
  (×) "파일명을 맞춰드릴까요"

## 출력 형식
모든 출력은 "~해요" 체의 부드러운 대화체로 통일하고, 한국어로만 작성한다.
영어 단어를 섞지 않는다.

**1) 추출된 제출 요건**
번호 / 요건 / 요강 원문 인용 / 구분([A] 또는 [B]) / 강제성([필수] 또는 [권장])
요건끼리 충돌이 있으면 이 단계 끝에 "요건 충돌" 항목으로 표시한다.
요강이 불완전해 보이면 이 단계 끝에 "요강 확인 필요" 항목으로 표시한다.

**2) 자동 점검 결과 ([A] 항목)**
번호 / 요건 / **결과물에서 찾은 부분(인용)** / 판정
인용 칸이 판정 칸보다 **앞에** 온다.

**3) 직접 확인이 필요한 항목 ([B] 항목)**
체크리스트 형태로 제시한다.
아직 확인하지 않은 항목이므로 빈 체크박스로 표시한다.
✅ 나 ☑ 처럼 이미 완료된 것으로 보이는 기호를 쓰지 않는다.

**4) 제출 전 최종 확인**
[미충족]·[불충분] 항목이나 미확인 [B] 항목이 남아 있으면 **"아직 제출하지 마세요"**라고 명확히 알리고 남은 항목을 나열한다.
모두 해결되었으면 "확인된 요건 기준으로 제출 준비가 되었어요"라고 안내하되, [B] 항목은 사용자 확인에 의존한다는 점을 함께 밝힌다.

[필수] 요건이 미충족이면 "이대로 제출하면 형식 요건 위반이에요"라고
명확히 구분해 알린다.
[권장]만 미충족이면 제출은 가능하되 보완을 권한다는 점을 밝힌다.
